<link rel="stylesheet" href="{{ 'component-rte.css' | asset_url }}" media="print" onload="this.media='all'">
<noscript>{{ 'component-rte.css' | asset_url | stylesheet_tag }}</noscript>

{% unless template.suffix contains 'landing' %}
<div class="page-width page-width--narrow{% if settings.banner_animation == 'effect_fade_up' %} scroll-trigger animate--slide-in{% endif %}">
	<div class="container">
	  <h1 class="main-page-title page-header{% if settings.banner_animation == 'effect_fade_up' %} scroll-trigger animate--slide-in{% endif %}">
	    {{ page.title | escape }}
	  </h1>
	  <div class="rte{% if settings.banner_animation == 'effect_fade_up' %} scroll-trigger animate--slide-in{% endif %}">
	   
        
	  </div>
  	</div>
</div>
{% endunless %}

{% schema %}
{
  "name": "faq",
  "tag": "section",
  "class": "spaced-section"
}
{% endschema %}

<style>
  .panel p strong { display: block; font-size: 14px; }
  .panel p { font-size: 13px; }
  .panel h5 { font-family: 'lato'; font-weight: 600; font-size: 14px; }
  .set > a.level2:before { position: absolute; content: '';
    background-image: url(https://cdn.shopify.com/s/files/1/0878/8813/4446/files/plus.png); width: 20px; height: 20px; z-index: 9999999; top: 18px; right: 25px;
    background-repeat: no-repeat; background-size: contain; border-radius: 50%; }
  .set a.level2.active:before { background-image: url(https://cdn.shopify.com/s/files/1/0878/8813/4446/files/minus.png); }
  .cont1 { display: none; padding: 0px 35px 14px 20px; background: #f7f7f7; }
  .set a.level2 { position: relative; display: block; padding: 16px 15px 17px 20px; text-decoration: none; color: #000 !important;
    -webkit-transition: all 0.2s linear; -moz-transition: all 0.2s linear; transition: all 0.2s linear; text-transform: capitalize;
    background: #f7f7f7; font-size: 16px; font-weight: 600; line-height: 20px; }
  .set:nth-child(odd) a.level2 { background: #eee; }
  .set:nth-child(even) a.level2 { background: #ccc; }
  table.table-striped { width: 100%; border: 1px solid #dddada; border-collapse: collapse; }
  table.table-striped td { padding: 5px 15px; border: 1px solid #d1d1d1; text-align: center; }
  table.table-striped th { padding: 10px 5px; border: 1px solid #d1d1d1; text-align: center; font-size: 13px; background: #ededed; }
  .panel a button { width: auto; display: table; }
  @media ( max-width:768px ) {
    h1.main-page-title.page-header { padding: 30px 0px 30px; font-size: 17px; }
    .set a.level2 { font-size: 15px; }
  }
</style>

<script> 
$(document).ready(function() {
    $(".set > a").on("click", function() {
    if ($(this).hasClass("active")) {
      $(this).removeClass("active");
      $(this).siblings(".cont1").slideUp(200);
    } else {
      $(".set > a").removeClass("active");
      $(this).addClass("active");
      $(".cont1").slideUp(200);
      $(this).siblings(".cont1").slideDown(200);
    }
  });
}); 
</script>